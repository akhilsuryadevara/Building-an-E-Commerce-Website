1) Install Tomcat,MySQl and MongoDB
2) copy the source code into apache tomcat folder inside the "webapps" directory
3) first initialize MySQL, MongoDB and then Apache Tomcat in their respective order
4) copy the given MySQL code into MySQL database("the file is given in the zip file with name "Query")
5) Open Firefox(Recommended) and enter "localhost/WebStore"
6) Experiment with it like a normal Amazon website.

That's it! Thanks!

